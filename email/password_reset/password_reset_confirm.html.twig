{%- extends '@email/base/base.html.twig' -%}
{%- trans_default_domain 'mail' -%}

{%- block subject -%}
  {{- 'password_reset_confirm.subject'|trans -}}
{%- endblock subject -%}

{%- block content -%}
  <row>
    <columns>
      <button href="{{- abs_url('password_reset_confirm', mpp()|merge({'token': token})) -}}" class="radius">
        {{- 'password_reset_confirm.btn_label'|trans -}}
      </button>
      <p>
        {{- 'password_reset_confirm.message'|trans({
            'system_name': config_str('system.name', schema),
        }) -}}
      </p>
      <p>
        {{- 'expiration.1_hour'|trans -}}
      </p>
      <p>
        {{- 'password_reset_confirm.explain'|trans({
          'system_name': config_str('system.name', schema),
          'ao': a_open('home', mpp()),
          'ac': '</a>'
        })|raw -}}
      </p>
    </columns>
  </row>
{%- endblock content -%}
