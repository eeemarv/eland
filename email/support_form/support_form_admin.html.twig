{%- extends '@email/base/base.html.twig' -%}
{%- trans_default_domain 'mail' -%}

{% block subject -%}
  {{- 'support_form_admin.subject'|trans -}}
{%- endblock subject -%}

{% block content_1row -%}
  <p>
    {{- 'support_form_admin.explain'|trans({
      'system_name': config_str('system.name'),
      'account': account(user_id),
      'ao': a_open('users_show', {'id': user_id}|mpp('admin')),
      'ac': '</a>',
    })|raw -}}
  </p>
  <p>
    {%- if can_reply -%}
      {{- 'support_form_admin.explain_reply'|trans -}}
    {%- else -%}
      {{- 'support_form_admin.explain_no_reply'|trans({
        'ao': a_open('users_show', {'id': user_id}|mpp('admin')),
        'ac': '</a>',
      })|raw -}}
    {%- endif -%}
  </p>
{%- endblock content_1row -%}

{%- block message_text -%}
  {{- message|nl2br -}}
{%- endblock message_text -%}

{%- block subscribe -%}
  support
{%- endblock subscribe -%}
