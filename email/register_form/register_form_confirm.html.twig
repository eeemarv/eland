{%- extends '@email/base/base.html.twig' -%}
{%- trans_default_domain 'mail' -%}

{%- block subject -%}
  {{- 'register_form_confirm.subject'|trans -}}
{%- endblock subject %}

{%- block content -%}
  <row>
    <columns>
      <button href="{{- abs_url('register_form_confirm', mpp()|merge({'token': token})) -}}" class="radius">
        {{- 'register_form_confirm.btn_label'|trans -}}
      </button>
      <p>
        {{- 'register_form_confirm.message'|trans -}}
      </p>
      <p>
        {{- 'expiration.1_day'|trans -}}
      </p>
      <p>
        {{- 'register_form_confirm.explain'|trans({
          'system_name': config_str('system.name', schema),
          'ao': a_open('register_form', mpp()),
          'ac': '</a>'
        })|raw -}}
      </p>
    </columns>
  </row>
{%- endblock content -%}
