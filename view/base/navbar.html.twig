{%- extends 'base/sidebar.html.twig' -%}
{%- import 'macro/menu.html.twig' as m -%}

{%- block navbar_toggle_button -%}
	{{- m.navbar_toggle_button() -}}
{%- endblock navbar_toggle_button -%}

{%- block navbar_collapse -%}
	<div class="collapse navbar-collapse" id="navbar-collapse-1">
	<ul class="nav navbar-nav navbar-right">

	{%- block navbar_menu -%}
	{#
		{%- if has_nav_menu('system') -%}
			{{- m.navbar_menu_dropdown_button('Systeem', 'share-alt', nav_menu('system')) -}}
		{%- endif -%}

		{%- if pp_role('user') or pp_role('admin') -%}
			{{- m.navbar_menu_dropdown_button(session_user_str, 'user', nav_menu_user()) -}}
		{%- endif -%}

		{%- if pp_role('guest') -%}
			{{- m.navbar_menu_dropdown_button(session_user_str, 'user', nav_menu_logout()) -}}
		{%- endif -%}
		#}

		{%- if pp_role('admin') -%}
			{{- m.navbar_menu_dropdown_button('Admin modus', 'cog', menu_nav_admin()) -}}
		{%- elseif s_role('admin') -%}
			<li>
				<a href="{{- path(global.request.attributes.get('_route'), global.request.attributes.all|merge({'role_short': 'a'})) -}}" title="Admin modus inschakelen">
					<i class="fa fa-times text-danger"></i>&nbsp;
					Admin modus
				</a>
			</li>
		{%- endif -%}
	{%- endblock navbar_menu -%}

	</ul>
	</div>
{%- endblock navbar_collapse -%}

{%- block sidebar_menu_append -%}
{#
	{%- if pp_role('admin') -%}
		{{- m.menu_admin_anon(menu_admin_anon('admin_anon')) -}}
	{%- endif -%}
#}
{%- endblock sidebar_menu_append -%}
