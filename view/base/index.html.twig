{%- extends 'base/base.html.twig' -%}

{%- block title -%}eLAND{%- endblock -%}
{%- block navbar -%}{%- endblock -%}

{%- block page_container -%}

	<div class="row">
		<div class="col-md-12">
			<div class="jumbotron">
				<div class="container">

				<h1>eLAND Index</h1>
				<p>Server gemeenschapsmunten</p>
				</div>
			</div>
		</div>
	</div>

	<div class="container-fluid">
		<div class="row">

		{%- if systems -%}
			{%- set systems_per_col = (systems|length / 3)|round(0, 'ceil') -%}

			{%- for s in systems -%}
				{%- if not (loop.index0 % systems_per_col) -%}
				<div class="col-md-4">
					<div class="panel panel-info">
						<ul class="list-group">
				{%- endif -%}
							<li class="list-group-item">
								<a href="{{- path('home_system', {'system': s}) -}}">{{- config('systemname', s) -}}</a>
							</li>
				{% if loop.last or not (loop.index % systems_per_col) -%}
						</ul>
					</div>
				</div>
				{%- endif -%}
			{%- endfor -%}

		{%- endif -%}
		</div>

	<div class="row">
	<div class="col-md-12">

	<div class="panel panel-info">
	<div class="panel-heading">

	<p>
		Hosting van gemeenschapsmuntsystemen op deze <a href="https://eland.letsa.net">eLAND server</a> wordt gratis aangeboden op basis van het
		principe van de 2 voeten: Gebruik het wanneer het nuttig is voor je. Indien niet, ga elders. Waarbij uw data steeds uw data blijft.
		Gebruik het <a href="{{- path('index_contact') -}}">contactformulier</a> bij interesse.
	</p>

	</div>
	</div>

	</div>
	</div>

	</div>

	<br><br>
{%- endblock page_container -%}

{%- block index_link -%}{%- endblock -%}
