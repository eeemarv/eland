{% extends 'mail/base/private_msg_intersystem.twig' %}
{%- trans_default_domain 'mail' -%}

{% block subject_line -%}
    {{- 'user_msg_intersystem.subject'|trans({
        '%from_account%': account(from_user.id, from_schema),
        '%from_system_name%': config('systemname', from_schema)
    }) -}}
{%- endblock subject_line %}

{% block html_explain -%}
    {{- 'user_msg_intersystem.explain'|trans({
        '%from_account%': account(from_user.id, from_schema),
        '%from_system_name%': config('systemname', from_schema)
    })|raw -}}
{%- endblock html_explain -%}

{% block text_explain %}
{{ 'user_msg_intersystem.explain'|trans({
    '%from_account%': account(from_user.id, from_schema),
    '%from_system_name%': config('systemname', from_schema)
}) }}
{% endblock text_explain %}
