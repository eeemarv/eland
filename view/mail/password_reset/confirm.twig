{%- extends 'mail/base/base.twig' -%}
{%- trans_default_domain 'mail' -%}

{%- block subject_line -%}
    {{- 'password_reset_confirm.subject'|trans -}}
{%- endblock subject_line %}

{%- block html_content -%}

<p>
    {{- 'password_reset_confirm.html_msg'|trans({
        '%link_open%': context_url_open(
            'password_reset_token', pp_ary, {'token': token, 'et': et}),
        '%link_close%': '</a>',
        '%system_name%': config('systemname', schema)
    })|raw -}}
</p>

<p>
    {{- 'expiration.1_day'|trans -}}
</p>

<p>
    {{- 'password_reset_confirm.explain'|trans({
        '%system_name%': config('systemname', schema),
        '%link_open%': context_url_open('home_system', pp_ary, {'et': et}),
        '%link_close%': '</a>'
    })|raw -}}
</p>
{%- endblock html_content -%}

{% block text_content %}
{% autoescape false %}

{{ 'password_reset_confirm.text_msg'|trans({
    '%system_name%': config('systemname', schema)
}) }}

{{ context_url('password_reset_token', mpp_anon_ary, {'token': token}) }}

{{ 'expiration.1_day'|trans }}

{{ 'password_reset_confirm.explain'|trans({
    '%system_name%': config('systemname', schema),
    '%link_open%': '',
    '%link_close%': ''
}) }}

{% endautoescape %}
{% endblock text_content %}
