{% trans_default_domain 'mail' %}

{{ 'subscribed.support'|trans({
    '%link_open%': '',
    '%link_close%': '',
})|raw }}

{{ context_url('config', pp_admin_ary,
    {'tab': 'mail-addr', 'et': et})|raw }}
