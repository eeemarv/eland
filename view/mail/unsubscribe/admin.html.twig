{%- trans_default_domain 'mail' -%}
<p>
    {{- 'subscribed.admin'|trans({
        '%link_open%': mail_link_open('config', {'active_tab': 'mailaddresses'}, schema),
        '%link_close%': '</a>',
    })|raw -}}
</p>
