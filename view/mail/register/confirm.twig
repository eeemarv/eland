{%- extends 'mail/base/base.twig' -%}

{%- block subject_line -%}
    {{- 'register_confirm.subject'|trans({
        '%system_name%': config('systemname', schema)}) -}}
{%- endblock subject_line %}

{%- block html_content -%}
{%- trans_default_domain 'mail' -%}

<p>
    {{- 'register_confirm.html_msg'|trans({
        '%link_open%': mail_link_open('register', {'token': token}, schema),
        '%link_close%': '</a>'
    })|raw -}}
</p>

<p>
    {{- 'expiration.1_week'|trans -}}
</p>

<p>
    {{- 'register_confirm.explain'|trans({
        '%system_name%': config('systemname', schema),
        '%link_open%': mail_link_open('register', {}, schema),
        '%link_close%': '</a>'
    })|raw -}}
</p>
{%- endblock html_content -%}

{% block text_content %}

{{ 'register_confirm.text_msg'|trans|raw }}

{{ mail_url('register', {'token': token}, schema)|raw }}

{{ 'expiration.1_week'|trans|raw }}

{{ 'register_confirm.explain'|trans({
    '%system_name%': config('systemname', schema),
    '%link_open%': '',
    '%link_close%': ''
})|raw }}

{{ mail_url('register', {}, schema)|raw }}

{% endblock text_content %}
