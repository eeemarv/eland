{% extends 'mail/base/news_admin.twig' %}
{%- trans_default_domain 'mail' -%}

{% block subject_line -%}
    {{- 'news_review_admin.subject'|trans({
        '%name%': news.headline
    })|raw -}}
{%- endblock subject_line %}

{% block html_content %}

<h3>
    {{- 'news_review_admin.title'|trans -}}
</h3>

<p>
    {{- 'news_review_admin.message'|trans({
        '%name%': news.headline
    }) -}}
</p>

<p>
    <a href="{{- mail_url('news', {'id': news.id}, schema, email_token) -}}">
        {{- 'news_review_admin.action'|trans -}}
    </a>
</p>

{% endblock html_content %}

{% block text_content %}
{% autoescape false %}

{{ 'news_review_admin.title'|trans|underline }}

{{ 'news_review_admin.message'|trans({
    '%name%': news.headline
}) }}

{{ 'news_review_admin.action'|trans }}

{{ mail_url('news', {'id': news.id}, schema, email_token) }}

{% endautoescape %}
{% endblock text_content %}
