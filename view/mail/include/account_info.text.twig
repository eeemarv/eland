{% trans_default_domain 'mail' %}
{{ 'label.your_account'|trans|underline }}

{{ 'label.login'|trans }}: {{ mail_url('login', {'login': user.letscode}, schema) }}

{{ 'label.account_code'|trans }}: {{ user.letscode }}

{{ 'label.username'|trans }}: {{ user.name }}

{{ 'label.balance'|trans }}: {{ user.saldo }} {{ config('currency', schema) }}
