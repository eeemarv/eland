{%- trans_default_domain 'mail' -%}
<h3>
    {{- 'b.support.title'|trans() -}}
</h3>

<p>
    {{ 'b.support.explain'|trans({
        '%link_open%': context_url_open('support',
            pp_ary_self(user_id, schema), {'et': et}),
        '%link_close%': '</a>',
        '%system_name%': config('systemname', schema)
    })|raw }}.
</p>
