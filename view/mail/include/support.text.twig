{% trans_default_domain 'mail' %}

{{ 'b.support.title'|trans|underline|raw }}

{{ 'b.support.explain'|trans({
    '%link_open%': '',
    '%link_close%': '',
    '%system_name%': config('systemname', schema)
})|raw }}:

{{ mail_url('support', {}, schema, email_token)|raw }}
