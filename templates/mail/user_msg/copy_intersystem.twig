{% extends 'mail/base/private_msg_copy.twig' %}
{%- trans_default_domain 'mail' -%}

{% block subject_line -%}
    {{- 'user_msg_copy_intersystem.subject'|trans({
        '%to_account%': account(to_user.id, to_schema),
        '%to_system_name%': config_str('system.name', to_schema)
    })|raw -}}
{%- endblock subject_line %}

{% block html_explain -%}
    {{- 'user_msg_copy_intersystem.explain'|trans({
        '%to_account%': account(to_user.id, to_schema),
        '%to_system_name%': config_str('system.name', to_schema)
    })|raw -}}
{%- endblock html_explain -%}

{% block text_explain %}
{{ 'user_msg_copy_intersystem.explain'|trans({
    '%to_account%': account(to_user.id, to_schema),
    '%to_system_name%': config_str('system.name', to_schema)
})|raw }}
{% endblock text_explain %}
