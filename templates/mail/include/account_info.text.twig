{% trans_default_domain 'mail' %}
{{ 'label.your_account'|trans|underline|raw }}

{{ 'label.login'|trans|raw }}: {{ context_url('login', mpp_anon_ary(schema),
    {'login': account_code(user_id)})|raw }}
{{ 'label.account_code'|trans|raw }}: {{ account_code(user_id)|raw }}
{{ 'label.username'|trans|raw }}: {{ username(user_id)|raw }}
{{ 'label.balance'|trans|raw }}: {{ account_balance(user_id) }} {{ config_str('transactions.currency.name')|raw }}
