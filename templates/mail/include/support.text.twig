{% trans_default_domain 'mail' %}

{{ 'b.support.title'|trans|underline|raw }}

{{ 'b.support.explain'|trans({
    'ao': '',
    'ac': '',
    'system_name': config_str('system.name', schema)
})|raw }}:

{{ context_url('support_form', mpp_ary(user_id, schema), {})|raw }}
