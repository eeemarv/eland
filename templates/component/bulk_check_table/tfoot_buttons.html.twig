<tfoot>
  <tr>
    <td colspan="30" class="bg-primary">
      <input type="button"
        data-action="click->bulk-check-table#selectAll"
        class="btn btn-info btn-sm me-1"
        value="{{- 'btn.select_all'|trans -}}" />
      <input type="button"
        data-action="click->bulk-check-table#selectInvert"
        class="btn btn-info btn-sm"
        value="{{- 'btn.select_invert'|trans -}}" />
    </td>
  </tr>
</tfoot>
