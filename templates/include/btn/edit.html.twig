<a href="{{- path(btn_route, {}|pp|merge({'id': id})) -}}" class="btn btn-primary"{%- if btn_title is defined %} title="{{- btn_title|trans(btn_title_trans_params|default({})) -}}"{%- endif -%}{%- for n, v in btn_attr|default({}) %} {{ n -}}="{{- v -}}"{%- endfor -%}>
  <twig:ux:icon name="lucide:pencil"/>
  &nbsp;
  {{- 'btn.edit'|trans -}}
</a>
