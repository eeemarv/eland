<div class="panel panel-default">
  <div class="panel-body">
    <input type="button"
      class="btn btn-default"
      data-table-sel="all"
      value="{{- 'btn.select_all'|trans -}}" />
    &nbsp;
    <input type="button"
      class="btn btn-default"
      data-table-sel="invert"
      value="{{- 'btn.select_invert'|trans -}}" />
  </div>
</div>
