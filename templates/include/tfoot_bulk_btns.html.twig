<tfoot>
  <tr>
    <td colspan="30" class="bg-primary">
      <input type="button"
        class="btn btn-info btn-sm"
        data-table-sel="all"
        value="{{- 'btn.select_all'|trans -}}" />
      &nbsp;
      <input type="button"
        class="btn btn-info btn-sm"
        data-table-sel="invert"
        value="{{- 'btn.select_invert'|trans -}}" />
    </td>
  </tr>
</tfoot>
