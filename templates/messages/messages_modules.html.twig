{%- extends 'messages/base/messages_form.html.twig' -%}

{%- block heading_title -%}
    {{- 'messages_modules.title'|trans -}}
{%- endblock heading_title -%}

{%- block form_a -%}
    {{- form_row(form.service_stuff_enabled, {
        'label': 'messages_modules.service_stuff_enabled.label',
        'required': false
    }) -}}

    {{- form_row(form.category_enabled, {
        'label': 'messages_modules.category_enabled.label',
        'required': false
    }) -}}

    {{- form_row(form.expires_at_enabled, {
        'label': 'messages_modules.expires_at_enabled.label',
        'required': false
    }) -}}

    {{- form_row(form.units_enabled, {
        'label': 'messages_modules.units_enabled.label',
        'required': false
    }) -}}
{%- endblock form_a -%}
