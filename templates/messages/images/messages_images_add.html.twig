{%- extends 'messages/images/messages_images_edit.html.twig' -%}

{%- block heading_title -%}
    {{- 'messages_images_add.title'|trans({
        'self': su_is_owner(user_id) ? 'self' : 'some_user',
        'user': account(user_id, schema),
        'subject': message.subject,
        'offer_want': message.offer_want,
    }) -}}
{%- endblock heading_title -%}

{%- block form_submit_btn -%}
    {%- include 'form/submit_add_btn.html.twig' -%}
{%- endblock form_submit_btn -%}
