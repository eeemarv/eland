<div class="row mb-5p">
  {{- form_row(filter_form.q, {
    'label': false,
    'col': 'sm-6',
    'addon': ux_icon('lucide:search'),
    'attr': {
      'placeholder': 'label.search',
    },
    'id': 'q',
  }) -}}

  {{- form_row(filter_form.type, {
    'label': false,
    'col': 'sm-6',
    'addon': 'label.type'|trans ~ ' '
      ~ ux_icon('lucide:rectangle-circle'),
    }
  ) -}}

</div>
<div class="row{%- if filter_form.access is defined %} mb-5p{%- endif -%}">

  {{- form_row(filter_form.ustatus, {
    'label': false,
    'col': filter_form.access is defined ? 'sm-6' : 'sm-5',
    'addon': 'label.status'|trans ~ ' '
      ~ ux_icon('lucide:user'),
    }
  ) -}}

  {{- form_row(filter_form.user, {
    'label': false,
    'col': filter_form.access is defined ? 'sm-6' : 'sm-5',
    'addon': ux_icon('lucide:user'),
    'attr': {
      'placeholder': 'label.account_code'
    }
  }) -}}

{%- if filter_form.access is defined -%}

</div>
<div class="row">

  {{- form_row(filter_form.access, {
    'label': false,
    'col': 'sm-10',
    'count_ary': count_ary
  }) -}}

{%- endif -%}

  {{- form_row(filter_form.show, {
    'label': 'btn.show',
    'col': 'sm-2',
    'attr': {
      'class': 'btn btn-default btn-block'
    }
  }) -}}
</div>
