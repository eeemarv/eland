{%- extends 'contacts/base/contacts_base.html.twig' -%}

{%- block heading_title -%}
    {%- if is_self -%}
        {{- 'contacts_del_self.title'|trans -}}
    {%- else -%}
        {{- 'contacts_del.title'|trans({
            'user': account(user_id, schema)|link('users_show', {'id': user_id})
        })|raw -}}
    {%- endif -%}
{%- endblock heading_title -%}

{%- block css -%}
    {{- parent() -}}
{%- endblock css -%}

{%- block js -%}
    {{- parent() -}}
{%- endblock js -%}