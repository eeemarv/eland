{%- extends 'contacts/base/contacts_form.html.twig' -%}
{%- import 'macro/lbl.html.twig' as lbl -%}

{%- block assets_add -%}
    {{- parent() -}}
    {{- assets_add(['contacts_del.js']) -}}
{%- endblock assets_add -%}

{%- if pp_has_role('admin') -%}
  {%- set admin_heading = true -%}
{%- endif -%}

{%- block heading_title -%}
  {{- 'contacts_del.title'|trans({
    'self': is_self or not user_id ? 'self' : 'some_user',
    'user': user_id ? account(user_id)|link('users_show', {'id': user_id}) : ''
  })|raw -}}
{%- endblock heading_title -%}

{%- block form_submit_btn -%}
  {%- include 'form/submit_del_btn.html.twig' -%}
{%- endblock form_submit_btn -%}

{%- block css -%}
  {{- parent() -}}
{%- endblock css -%}

{%- block js -%}
  {{- parent() -}}
{%- endblock js -%}