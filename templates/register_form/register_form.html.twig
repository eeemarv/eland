{%- extends 'register_form/base/register_form_base.html.twig' -%}

{%- block content -%}
{%- endblock content -%}

{%- block form_a -%}
  {{- form_row(form.email, {
    'label': 'label.your_email',
    'help': 'help.email_validation_link',
    'addon': ux_icon('lucide:mail'),
  }) -}}

  {{- form_row(form.first_name, {
    'label': 'label.first_name',
    'addon': ux_icon('lucide:user'),
  }) -}}

  {{- form_row(form.last_name, {
    'label': 'label.last_name',
    'addon': ux_icon('lucide:user'),
  }) -}}

  {{- form_row(form.postcode, {
    'label': 'label.postcode',
    'addon': ux_icon('lucide:map-pin'),
  }) -}}

  {{- form_row(form.mobile, {
    'label': 'label.mobile',
    'sub_label': 'label.optional',
    'addon': ux_icon('lucide:smartphone'),
    'required': false
  }) -}}

  {{- form_row(form.phone, {
    'label': 'label.phone',
    'sub_label': 'label.optional',
    'addon': ux_icon('lucide:phone'),
    'required': false
  }) -}}

  {%- include 'form/captcha.html.twig' -%}
{%- endblock form_a -%}

{%- block form_cancel_btn -%}
{%- endblock form_cancel_btn -%}

{%- block form_submit_btn -%}
  {%- include 'form/submit_edit_btn.html.twig' with {
    'label': 'btn.register'
  } -%}
{%- endblock form_submit_btn -%}
