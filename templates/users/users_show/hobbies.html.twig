{%- if not is_intersystem and config_bool('users.fields.hobbies.enabled', schema) -%}
{%- embed 'embed/list_item.html.twig' -%}
    {%- import 'macro/lbl.html.twig' as l -%}
    {%- block dl -%}
        <dt>
            {{- 'label.hobbies'|trans -}}
        </dt>
        <dd>
            {{- user.hobbies|no_content -}}
        </dd>
    {%- endblock dl -%}
    {%- block div_right -%}
        {%- if pp_is_admin() or is_self -%}
            {%- include 'include/btn/edit.html.twig' with {
                'btn_route': 'users_hobbies_edit' ~ (is_self ? '_self' : ''),
                'btn_route_params': is_self ? {} : {'id': id},
                'btn_title': 'users_hobbies_edit.title',
                'btn_title_trans_params': {
                    'user': account(id, schema),
                    'self': is_self ? 'self' : 'some_user'
                },
                'btn_label_hidden': 'sm',
                'btn_lg': true,
            } -%}
        {%- endif -%}
    {%- endblock div_right -%}
{%- endembed -%}
{%- endif -%}
