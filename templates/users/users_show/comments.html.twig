{%- if config_bool('users.fields.comments.enabled', schema) -%}
{%- embed 'users/users_show/list_item.html.twig' -%}
    {%- block item -%}
        <dt>
            {{- 'label.comments'|trans -}}
        </dt>
        <dd>
            {{- user.comments|no_content -}}
        </dd>
    {%- endblock item -%}
    {%- block btn -%}
        {%- if pp_is_admin() or su_is_owner(id) -%}
            {%- include 'include/btn/edit.html.twig' with {
                'btn_route': 'users_comments_edit' ~ (su_is_owner(id) ? '_self' : ''),
                'btn_title': 'users_comments_edit.title',
                'btn_title_trans_params': {
                    'user': account(id, schema),
                    'self': is_self ? 'self' : 'some_user'
                },
                'btn_label_hidden': 'sm',
                'btn_lg': true,
            } -%}
        {%- endif -%}
    {%- endblock btn -%}
{%- endembed -%}
{%- endif -%}
