{%- extends 'users/base/users_single_form.html.twig' -%}

{%- block heading_title -%}
    {{- 'users_hobbies_edit.title'|trans({
        'self': is_self ? 'self' : 'some_user',
        'user': account(id, schema)
    }) -}}
{%- endblock heading_title -%}

{%- block form_a -%}
    {{- form_row(form.hobbies, {
        'label': 'label.hobbies',
        'attr': {
            'rows': 5
        }
    }) -}}
{%- endblock form_a -%}
