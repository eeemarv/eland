{%- extends 'users/base/users_single_base.html.twig' -%}

{%- block heading_fa -%}
    key
{%- endblock heading_fa -%}

{%- block heading_title -%}
    {%- if is_self -%}
        {{- 'users_password_edit_self.title'|trans -}}
    {%- else -%}
        {{- 'users_password_edit.title'|trans({
            '%user%': account(id, schema)|link('users_show', {'id': id})
        })|raw -}}
    {%- endif -%}
{%- endblock -%}

{#
{%- block content -%}

{%- embed 'form/form_embed.html.twig' -%}
    {%- import 'macro/link.html.twig' as l -%}
    {%- block form_content -%}

    {{- form_row(form.password, {
        'label': 'label.new_password',
        'help': 'help.new_password_safety'
    }) -}}

    {{- form_row(form.notify, {
        'label': 'label.send_notification_email',
        'disabled': not notify_enabled,
        'label_attr': {'class': 'checkbox-custom'}
    }) -}}

    {{- form_widget(form.submit,{
        'label': 'btn.save_password',
        'attr': {'class': 'btn-primary'}
    }) -}}

    {%- endblock form_content -%}
{%- endembed -%}
{%- endblock content -%}
#}

{%- block css -%}
    {{- parent() -}}
{%- endblock css -%}

{%- block js -%}
    {{- parent() -}}
    {{- encore_entry_script_tags('generate_password') -}}
{%- endblock js -%}